{{define "root"}}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width">
        <title>{{ .Title }}{{ .Cfg.Title}}</title>
        
    </head>
    <body>
    {{ .Content }}
    {{ .Append }}
    </body>
</html>

{{end}}
